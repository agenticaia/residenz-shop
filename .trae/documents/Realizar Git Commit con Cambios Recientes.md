He actualizado el mensaje del commit a español como solicitaste.

**Archivos a incluir:**

* Correcciones de UI en modales (`ServiceDetailImproved.tsx`, `Services.tsx`, `ServiceDetail.tsx`).

* Redirección en servidor (`server/index.ts`).

* Documentación API (`APIdocument.md`).

* Configuración MCP (`.vscode/settings.json`).

* Reorganización de archivos (`agent-tool.md`).

**Plan de Ejecución:**

1. Ejecutar `git add .`
2. Realizar el commit con el siguiente mensaje en español:

```text
chore: actualizar configuración, docs y corregir UI de modales

- fix(ui): mejorar responsividad y alineación de modales en móvil/escritorio
- feat(server): agregar redirección de raíz a ruta base /residenz-shop/
- docs: agregar documento de especificación de API (APIdocument.md)
- config: agregar configuración de servidores MCP para VSCode
- chore: mover agent-tool.md a carpeta docs
```

