**Limpieza Profunda de UI y Estandarización**

Voy a aplicar las guías de estilo 'Residenz Research' para unificar la estética y mejorar la profesionalidad del sitio.

**Acciones por Archivo:**

1.  **`client/src/components/Layout.tsx`:**
    *   **Navbar:** Ajustaré el botón "Empezar" a `rounded-xl` para armonía.
    *   **Footer:** Rediseñaré el CTA "Agendar Prueba" usando Cyan Trust (`#00BDD6`) y `rounded-xl` para máxima legibilidad y consistencia.

2.  **`client/src/pages/Home.tsx`:**
    *   **Hero:** Confirmaré que el botón principal sea `rounded-full` y `h-[56px]`.
    *   **Secciones (Comparativo, Garantía, Cierre):**
        *   Cambiaré todos los botones secundarios a `rounded-xl`.
        *   Suavizaré las tarjetas: `shadow-sm` y bordes sutiles `#E2E8F0`.
        *   Aumentaré el espaciado entre secciones a `py-16` o `py-20` (64px-80px) para asegurar los 48px mínimos y dar aire.

3.  **`client/src/components/home/HomeComponents.tsx`:**
    *   **Testimonios y Pasos:** Actualizaré los bordes a `rounded-xl` y reduciré las sombras (`shadow-xl` -> `shadow-sm`) para un look más limpio.

**Colores:** Usaré los tokens `#6668F2` (Primary) y `#00BDD6` (Accent) estrictamente donde corresponda.

¿Procedo con esta limpieza visual?